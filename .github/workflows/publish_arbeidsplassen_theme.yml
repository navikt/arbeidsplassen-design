name: Publish Arbeidsplassen theme package to GPR
on:
  workflow_dispatch:

jobs:
  publish:
    permissions:
      packages: write
      contents: write
      actions: read
    name: Publish package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4
        name: Install pnpm
        with:
          run_install: false
      - uses: actions/setup-node@v6
        with:
          node-version-file: '.nvmrc'
      - name: Configure registry and publish
        run: |
          pnpm config set @navikt:registry https://npm.pkg.github.com
          pnpm config set //npm.pkg.github.com/:_authToken $NODE_AUTH_TOKEN
          pnpm --filter @navikt/arbeidsplassen-theme publish --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
