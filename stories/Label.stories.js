import React from "react";
import Label from "../src/Label/Label";

export default {
    title: "Typography/Label",
    component: Label,
};

export const Default = (args) => <Label {...args}>One morning</Label>;
